{
   // See https://go.microsoft.com/fwlink/?LinkId=733558
   // for the documentation about the tasks.json format
   "version": "2.0.0",   
   "tasks": [
        {
            "label": "abi",
            "type": "shell",
            "command": "${env:TEOS} generate abi ${env:CONTRACTS_DIR}/@contract_name@;${env:TEOS} build contract ${env:CONTRACTS_DIR}/@contract_name@",
            "problemMatcher": [],
            "presentation": {
                    "reveal": "always",
                    "panel": "dedicated"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }            
        }       
   ]
}